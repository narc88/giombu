jQuery(function($) {
	/*	$("form").submit(function() {
			alert($('#userImageInput').val());
			$('#uploadedImage').attr('src', '');
   			$('#uploadedImage').show( "fold", 1000 );
		});
        $('#uploadedImage').Jcrop();*/
    });