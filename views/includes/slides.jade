div(class="carousel slide", data-ride="carousel")
	.carousel-inner
		-for (var i = images.length - 1; i >= 0; i--)
			-var image = images[i]
			.item
				img(src="/photos/users/#{image.filename}" , alt="#{image.filename}")